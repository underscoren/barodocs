const { Item, parseItemFile } = require("./Item");
const { Affliction, parseAfflictionFile } = require("./Affliction");

module.exports = {
    Item,
    parseItemFile,
    Affliction,
    parseAfflictionFile,
};